import ContactRequest from "../../models/contactRequest";

class contactSerices {
    /**
     * @description: Users Contact info for Query
     * @param {*} data
     * @param {*} req
     * @param {*} res
     */
    static async contactForm(data, req, res) {
        
    }
}

export default contactSerices;
